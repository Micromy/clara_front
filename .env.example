# Copy this file to .env and fill in the values. .env is gitignored.

# ── Frontend (Vite) ─────────────────────────────────────────────
# Backend REST endpoint. 빌드 타임에 inline됨.
# 사내 배포 환경에선 ConfigMap → BuildConfig env로 주입.
VITE_API_BASE=http://at-django--at-backend-django-prod.khprdpb01.apps.dks.samsungds.net
